# Secrets for running the pipeline in the GPU container.
# This file is GITIGNORED - do NOT commit it. `source` it (or let run_in_container.sh load it).
#
# WANDB_API_KEY: Avital's shared W&B key for this project's runs (she published it for the team).
#   All runs log to her "plate-classification" project.
# HF_TOKEN:      your Hugging Face read token (needed only for step 02 / FLUX.1-dev, a gated model).
#   Get one at https://huggingface.co/settings/tokens and paste it below.

export WANDB_API_KEY="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"
export HF_TOKEN=""

# Optional: rename your W&B run / project (otherwise defaults are used).
# export WANDB_RUN_NAME="shlomi_resnet18_frozen"
# export WANDB_PROJECT="plate-classification"
